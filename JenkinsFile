pipeline {
    agent any

    environment {
        PLAYWRIGHT_INSTALL_PATH = '/usr/local/lib/node_modules/playwright'
    }

    stages {
        stage('install playwright') {
            steps {
                script {
                    sh '''
                        /usr/local/bin/npm install -g @playwright/test
                        /usr/local/bin/npx playwright install
                    '''
                }
            }
        }
        stage('help') {
            steps {
                sh '/usr/local/bin/npx playwright test --help'
            }
        }
        stage('test') {
            steps {
                sh '''
                    /usr/local/bin/npx playwright test --list
                    /usr/local/bin/npx playwright test tests/NopCommerce
                '''
            }
            post {
                success {
                    archiveArtifacts(artifacts: 'homepage-*.png', followSymlinks: false)
                    sh 'rm -rf *.png'
                }
            }
        }
    }
}
